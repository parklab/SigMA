#' Generates summary plot 
#'
#' @param file the csv file produced by SigMA
#' @param detailed boolean determines whether a detailed 
#' summary plot or a short summary should be produced
#'

plot <- function(file = NULL, detailed = F){
  df <- read.csv(file)
  print(colnames(df)[grep('Signature_*_c', colnames(df))])
}